{
  "name": "kodama-app",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "cd ../ui && bun run dev",
    "build": "cd ../ui && bun run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
